services:
  - type: web
    name: electronics-inventory
    runtime: static
    buildCommand: npm run build
    staticPublishPath: artifacts/electronics-inventory/dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
